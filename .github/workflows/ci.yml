name: CI

on:
  push:
  pull_request:

  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  lua:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: leafo/gh-actions-lua@v13
        with:
          luaVersion: "5.1"

      - uses: leafo/gh-actions-luarocks@v6

      - name: Install Lua development tools
        run: |
          eval "$(luarocks path --lua-version=5.1 --bin)"
          make deps

      - uses: JohnnyMorganz/stylua-action@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          version: latest
          args: --check readeck.koplugin/main.lua readeck.koplugin/_meta.lua readeck.koplugin/readeck spec

      - name: Lint and test
        run: |
          eval "$(luarocks path --lua-version=5.1 --bin)"
          make lint
          make test

  koreader-runtime-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v6

      - name: Resolve latest KOReader tag
        id: koreader
        run: |
          tag="$(
            git ls-remote --tags --refs https://github.com/koreader/koreader.git 'v*' \
              | awk -F/ '{ print $NF }' \
              | sort -V \
              | tail -n 1
          )"
          if [ -z "$tag" ]; then
            echo "Could not resolve latest KOReader tag." >&2
            exit 1
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "Using KOReader $tag"

      - name: Clone KOReader latest tag
        run: |
          mkdir -p references
          git clone --depth 1 --branch "${{ steps.koreader.outputs.tag }}" https://github.com/koreader/koreader.git references/koreader

      - name: Cache KOReader emulator runtime
        id: koreader-runtime-cache
        uses: actions/cache@v5
        with:
          path: |
            references/koreader/base/build/x86_64-pc-linux-gnu-debug
            references/koreader/koreader-emulator-x86_64-pc-linux-gnu-debug
          key: koreader-runtime-${{ runner.os }}-${{ steps.koreader.outputs.tag }}

      - name: Install KOReader build dependencies
        if: steps.koreader-runtime-cache.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y \
            autoconf \
            automake \
            build-essential \
            ca-certificates \
            ccache \
            cmake \
            curl \
            gcc-multilib \
            gettext \
            git \
            libegl-dev \
            libtool \
            libtool-bin \
            libwayland-dev \
            libx11-dev \
            libxcursor-dev \
            libxext-dev \
            libxi-dev \
            libxkbcommon-dev \
            libxrandr-dev \
            libxss-dev \
            libxtst-dev \
            meson \
            nasm \
            ninja-build \
            patch \
            pkg-config \
            unzip \
            wget

      - name: Cache KOReader downloads
        if: steps.koreader-runtime-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v5
        with:
          path: references/koreader/base/thirdparty/*/build/downloads
          key: koreader-downloads-${{ runner.os }}-${{ steps.koreader.outputs.tag }}-${{ hashFiles('references/koreader/base/thirdparty/**/*.mk') }}
          restore-keys: |
            koreader-downloads-${{ runner.os }}-${{ steps.koreader.outputs.tag }}-
            koreader-downloads-${{ runner.os }}-

      - name: Cache ccache
        if: steps.koreader-runtime-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v5
        with:
          path: ~/.cache/ccache
          key: koreader-ccache-${{ runner.os }}-${{ steps.koreader.outputs.tag }}-${{ github.sha }}
          restore-keys: |
            koreader-ccache-${{ runner.os }}-${{ steps.koreader.outputs.tag }}-
            koreader-ccache-${{ runner.os }}-

      - name: Fetch KOReader third-party sources
        if: steps.koreader-runtime-cache.outputs.cache-hit != 'true'
        run: ./kodev fetch-thirdparty
        working-directory: references/koreader

      - name: Build KOReader emulator runtime
        if: steps.koreader-runtime-cache.outputs.cache-hit != 'true'
        run: ./kodev build emulator
        working-directory: references/koreader

      - name: Validate KOReader emulator runtime
        run: test -x references/koreader/koreader-emulator-x86_64-pc-linux-gnu-debug/koreader/luajit

      - name: KOReader runtime smoke test
        run: make koreader-runtime-smoke

      - name: KOReader network smoke test
        run: make koreader-network-smoke
